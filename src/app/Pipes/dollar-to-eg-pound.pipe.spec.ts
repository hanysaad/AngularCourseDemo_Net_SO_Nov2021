import { DollarToEgPoundPipe } from './dollar-to-eg-pound.pipe';

describe('DollarToEgPoundPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarToEgPoundPipe();
    expect(pipe).toBeTruthy();
  });
});
